# Title
Создать MarkdownGenerator

# Description
Создать НОВЫЙ модуль MarkdownGenerator в /src/formatters/generator.ts.

Функция: generate(id: string, data: TaskData, indexData?: IndexData): string

Q&A:
- TaskData interface: Новый тип с strict typing { title: string; description?: string; [key: string]: string | undefined }
- Relationship with Parser.serialize(): Замена, старый код будет удалён
- Dependencies format: Comma-separated (1, 2, 5)
- Missing task in indexData: Skip Dependencies section
- undefined indexData: Skip Dependencies section  
- Custom field keys: LOWERCASE (как указано в задаче)
- Field ordering: Title → Description → Dependencies → Custom (alphabetically)
- Empty/undefined custom fields: Skip section entirely
- Missing title: Throw error (required field)
- Missing description: Skip Description section (optional)
- Validation: Basic (проверка title, валидация типов)
- Error class: GenerationError в /src/formatters/errors.ts
- Test coverage: Unit + integration + edge cases
- Snapshot testing: Да (vitest snapshots)
- Imports: Types only (IndexData from index/types.ts)
- Class vs function: Simple function export

Output: Плоская markdown (# Key затем значение). Status НЕ включать. Dependencies из indexData. Custom fields lowercase.

# status
pending
