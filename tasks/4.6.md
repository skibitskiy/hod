# Title
Обновить команду list

# Description
Изменить list для работы с JSON.

Q&A:
- Format support: Migration - JSON only (breaking change)
- JSON schema: Flat ParsedTask-like { title, description?, customFields... }
- Storage interface: Change Task to { id, data: TaskData }
- Filter syntax: Flat only, top-level fields (no nested paths)
- Array filtering: Contains match (tags=bug matches if "bug" in array)
- Table complex types: Arrays as comma-separated, objects as JSON string
- JSON output: Normalized structure matching current outputJson()
- Error messages: Specific messages for different error types
- Mixed formats: JSON wins (if both exist, use JSON)
- Dependencies in JSON: Top-level field (from index read, not stored)
- Parser behavior: Extension-based (.md vs .json)
- Other commands: Update all commands simultaneously (consistent behavior)

storage.list() возвращает Task[] с data: TaskData. Фильтрация по data.fields (flat only). Graceful degradation — warning в console.error, skip task. Status/dependencies из index.

# status
pending
