# Title
Обновить команду list

# Description
Изменить команду list для работы с JSON файлами.

**Требования от Storage:**
- `storage.list()` — вернуть список всех задач с их JSON контентом
- Формат возврата: `Array<{ id: string, content: string }>` где content = JSON строка

**Входные данные:** Фильтры опционально
**Выходные данные:** Отфильтрованный и отсортированный список задач

**Новые методы:**
- Прочитать все JSON файлы
- Распарсить каждый JSON в ParsedTask (через новый метод парсера)
- Применить фильтры
- Вывести в таблицу/json/tree формате

# status
done
