# Title
Удалить Parser

# Description
Удалить модуль Parser ПОЛНОСТЬЮ.

Q&A:
- Task completion order: Complete ALL of task 4 (JSON migration) → then delete Parser
- MarkdownGenerator status: Task 4.1 must be complete before this task
- Replacement type: Create TaskData in /src/types.ts (move ParsedTask functionality)
- Field naming: Keep lowercase (task.title, task.description)
- File deletion order: Delete /src/parser/ directory entirely (rm -rf)
- Barrel export: Delete /src/parser/index.ts entirely
- Test files: Rewrite all fixtures to use JSON data directly
- Parser tests: Delete /src/parser/parser.test.ts entirely
- Services interface: Remove parser, add formatter: MarkdownGenerator
- Commands using parser: Must be migrated to JSON before this task
- Verification: TypeScript compilation check (imports from /src/parser will fail)
- Rollback plan: Git revert is sufficient
- Documentation: Update CLAUDE.md (remove parser section, add formatters)
- Existing .md files: Assume migrate has been run, fail fast if .md found
- Auto-migration: No, users must run hod migrate manually

Удалить /src/parser/, /src/parser/parser.ts, /src/parser/parser.test.ts, /src/parser/types.ts. Удалить импорты ParserService. Удалить outputMarkdown() из get.ts. Parser используется только в migrate — после migrate можно удалить.

# status
pending
