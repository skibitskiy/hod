# Title
Обновить команду get

# Description
Изменить get для работы с JSON. storage.read() возвращает { id, data }.

Q&A:
- data type: TaskData interface { title: string; description?: string; [key: string]: string | undefined }
- Parser usage: Remove ParserService completely, access data directly
- MarkdownGenerator signature: generate(id, data, indexEntry) - pass all three
- Non-string values: Skip non-string fields silently
- JSON output schema: Exact same schema - always strings
- Field name storage: kebab-case в JSON (как указано в 4.3)
- Missing title: Throw error
- Missing from index: Throw error
- Empty vs undefined: Skip if falsy
- Storage interface: { id, data: TaskData }
- Test mocks: Minimal JSON objects
- Config mapping: Use config to transform keys
- Null values: Treat as empty string
- Display order: ID, Title, Status, Dependencies, Description, custom (alphabetically)

Опция --markdown → MarkdownGenerator.generate(data, indexEntry). Остальные опции читают из data напрямую. Status/dependencies из index.

# status
pending
