{
  "title": "migrate должен перезаписывать JSON",
  "description": "Команда migrate должна перезаписывать существующий JSON файл, а не бросать ошибку. Добавить опцию --force для перезаписи или сделать это поведением по умолчанию."
}
