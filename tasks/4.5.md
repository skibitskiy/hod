# Title
Обновить команду update

# Description
Изменить update для работы с JSON.

Q&A:
- Storage interface: Modify update.ts to expect storage.read() → { id, data }
- Empty string semantics: Empty strings ARE included (as task spec states)
- Parser role: No parser - direct JSON manipulation
- Status/deps location: Index-only (consistent with 4.2/4.3)
- Field naming: CLI names/lowercase (title, description, custom-field)
- Rollback: Store original JSON object before changes
- Validation: Validate only changed fields
- Non-existent fields: Create the field (additive update)
- Partial update: Shallow merge { ...existing, ...new }
- Custom fields empty: Always include (consistent with task spec)

Читает { id, data } через storage.read(), обновляет указанные поля (shallow merge), передаёт в storage.update(id, data). Пустые значения ВКЛЮЧАТЬ.

# status
pending
