# Title
Обновить команду add

# Description
Изменить add для создания JSON файлов.

Q&A:
- Storage interface: Изменить create(id: string, data: Record<string, unknown>)
- Empty fields: Включать поля с defaults или значениями, опускать если нет ни того ни другого
- Index method: Использовать существующий index.update() (addTask не нужен)
- Custom fields keys: kebab-case (из field.name в конфиге)
- Status in JSON: Только в index, НЕ в JSON файле
- Defaults timing: Применять defaults при построении JSON (поля с defaults всегда включены)

Формирует JsonObject { title, description: "", customFields... } (пустые поля ВКЛЮЧАЁМ). Передаёт в storage.create(id, data). Status/dependencies → index.update(). Required: title → ошибка если отсутствует.

# status
pending
